name: Budowanie APK z luźnych plików (Wersja Java 17)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # 1. Pobranie plików z Twojego nowego repozytorium GitHub
    - name: Checkout repository
      uses: actions/checkout@v4

    # 2. Instalacja Node.js
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    # 3. Instalacja narzędzia Cordova
    - name: Install Cordova
      run: npm install -g cordova

    # 4. Tworzenie projektu i kopiowanie luźnych plików gry
    - name: Prepare Project
      run: |
        cordova create my-app com.example.robloxapp "RobloxFanApp"
        cd my-app
        cordova platform add android
        
        # Czyszczenie domyślnych plików Cordovy
        rm -rf www/*
        
        # Kopiowanie Twoich plików z repozytorium do folderu www aplikacji
        cp -r ../* www/ 2>/dev/null || true
        rm -rf www/my-app

    # 5. NOWA KONFIGURACJA JAVY (Naprawia błąd z Gradle)
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    # 6. Kompilacja i sprawdzenie błędów
    - name: Build Android APK
      run: |
        cd my-app
        cordova build android --debug
        
